"""Soul 10 — Kern. Code, der ein Modell umgibt; nicht Text, den ein Modell liest.

Befund: Auflagen als Text kommen zu 97,8 % an und werden zu 63,9 % erfüllt (01-BEFUNDE, E7);
ein Mechanismus im Code kennt diesen Abstand nicht — deshalb liegt hier Code und kein Prompt.
Erz → Gold: SOUL.md und der ANIMA-Kernel trugen die Regeln als Text, den ein Modell lesen und
vergessen konnte. Hier trägt jedes Modul unter core/ genau einen Mechanismus, und jeder schreibt
eine Bus-Zeile.

Siehe ENTSCHEIDUNG.md (warum) und ARCHITEKTUR.md (wie)."""
